<?js
    var data = obj;
    var self = this;
    var $ = this.theme.util;
    print($.list(data, item => {
        return $.tag({
            name: 'span',
            attrs: { class: 'param-type' },
            content: self.linkto(item, self.htmlsafe(item))
        });
    }));
?>
